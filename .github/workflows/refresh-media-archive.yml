name: Refresh Media Archive

on:
  schedule:
    - cron: "0 18 * * 1"
      timezone: America/Chicago
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: refresh-media-archive
  cancel-in-progress: false

jobs:
  refresh:
    # After cutover, the unified workflow owns both archives and every deployment.
    if: vars.DOCUMENT_ARCHIVES_ENABLED != 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.repository.default_branch }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Refresh archive and rebuild site
        run: npm run refresh:archive

      - name: Commit refreshed site
        run: |
          if [ -z "$(git status --porcelain)" ]; then
            echo "No changes detected."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A

          if git diff --cached --quiet; then
            echo "No staged changes detected."
            exit 0
          fi

          git commit -m "chore: refresh media archive"
          git push origin HEAD:$GITHUB_REF_NAME
