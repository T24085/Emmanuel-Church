# Optional local testing only. Production builds always use the real Firebase project.
NEXT_PUBLIC_FIREBASE_EMULATORS=false

# Build-time only. Dedicated key restricted to Google Drive API, never NEXT_PUBLIC.
# Required for npm run sync:documents; use the same name as a GitHub Actions secret.
DRIVE_ARCHIVE_API_KEY=
